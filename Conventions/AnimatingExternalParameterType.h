#pragma once

namespace Animating
{
    // Parameters used for the sampling of circle blenders 
    // and calculation of speed modifiers of animation states.    
    enum struct ExternalParameterType { None, HorizontalMovement, CameraAngle, RotationalOffset };
}